<h1>Editar Livro</h1>

<form action="/books/updatebook" method="POST" class="form">
    <input type="hidden" name="id" value="{{book.id}}">
    <div class="form-control">
        <label for="title">Título</label>
        <input type="text" name="title" placeholder="Insira o títlulo" value="{{book.title}}">
    </div>
        <div class="form-control">
        <label for="pagety">Numero de páginas</label>
        <input type="number" name="pagety" placeholder="Quantas páginas o livro tem?" value="{{book.pagety}}">
    </div>
    <input type="submit" value="Editar">
</form>